- content_tag_for(:tr, usr, :class => cycle(:odd, :even)) do
  %td.user_id= h usr.try(:user_id)
  %td.string= h usr.try(:string)
  %td.string= h usr.try(:string)
  %td.string= h usr.try(:string)
  %td.dob= h usr.try(:dob)
  %td.is_dp_gravatar= h usr.try(:is_dp_gravatar)
  %td.actions
    = link_to 'Show', usr_path(usr)
    |
    = link_to 'Edit', edit_usr_path(usr)
    |
    = link_to 'Destroy', usr, :confirm => 'Are you sure?', :method => :delete