%h1.heading
  = "Editing education %s" % @education.id
  
- semantic_form_for(@education) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', educations_path
