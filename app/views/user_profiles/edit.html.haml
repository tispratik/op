%h1.heading
  = "Editing user_profile %s" % @user_profile.id
  
- semantic_form_for(@user_profile) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', user_profiles_path
