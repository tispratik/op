= show_flash_messages

- if @comment.new_record?
  $('#comment_errors').html("#{escape_javascript(@comment.errors.full_messages.join("<br/>"))}")
- else
  $('#comments').append("#{escape_javascript(render @comment)}")
  $('#comment_errors').hide()
  $('#comment_message').val('')
